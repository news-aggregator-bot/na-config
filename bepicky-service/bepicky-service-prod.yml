na:
  schedule:
    sync:
      enabled: true
      cron: '*/2 * * * * *'
    refresh-id:
      cron: '* * */1 * * *'
    notify:
      enabled: true
      cron: '0 * * * * *'
    webpagereader:
      cron: '0 0 */12 * * *'